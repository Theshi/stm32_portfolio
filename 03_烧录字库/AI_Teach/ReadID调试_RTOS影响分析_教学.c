/**
 * ============================================================================
 * ReadID 返回 0xFFFF/0xFFFFFF 的 RTOS 影响分析
 * ============================================================================
 *
 * 【你的两个观点】
 *   1. W25Q64 初始化放在了 Task 里面 → 会有影响吗？
 *   2. 任务切换可能打断 SPI 读取 → 需要加 vTaskDelay(20) 吗？
 *
 * 【结论先讲】
 *   观点1：在当前代码中不存在（看下文分析）
 *   观点2：方向对了，但 vTaskDelay 不是治本的方法
 *
 *   真正要排查的是 CS 信号是否真的拉低了，以及 SPI 模式配置是否正确。
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │         第一章：你的代码到底在哪初始化了 W25Q64？               │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 你当前 main.c 的执行顺序：
 *
 *   int main(void)
 *   {
 *       // ...
 *       USART_Config();                                  // ① 串口初始化
 *       W25Q64_Init();                                   // ② Flash 硬件初始化 ← 在这里！
 *            └→ SPI_Flash_Init()   (GPIO、SPI 模式配置)
 *            └→ CS_HIGH()          (CS 默认拉高)
 *
 *       W25Q64_ReadID();                                 // ③ 读取 ID ← 也在这里！
 *            └→ SPI_Flash_ReadID()
 *                 └→ CS_LOW() → 发 0x9F → 收 ID → CS_HIGH()
 *
 *       printf("Flash ID = 0x%X\n", id);                 // ④ 打印结果
 *
 *       xTaskCreate(StartUpTask, ...);                   // ⑤ 创建任务
 *       xTaskCreate(LoaderTask,  ...);
 *       vTaskStartScheduler();                           // ⑥ 启动调度器 ← 整个 RTOS 才算开机
 *       // =========== 调度器启动之前，没有任务切换！===========
 *       while (1);
 *   }
 *
 *   ┌────────────────────────────────────────────────────────────────┐
 *   │                                                                 │
 *   │   ★ 关键发现：W25Q64_Init() 和 ReadID() 都在 vTaskStartScheduler() │
 *   │     之前执行，此时 FreeRTOS 还没开机，根本不存在"任务切换"！   │
 *   │                                                                 │
 *   │   你的观点1 "初始化放在 Task 里" → 实际上不在 Task 里，在 main │
 *   │   你的观点2 "任务切换打断"     → 此时根本没有任务可切换        │
 *   │                                                                 │
 *   └────────────────────────────────────────────────────────────────┘
 *
 * 那你 StartUpTask 里的 W25Q64_FileSysInit() 呢？
 *   → 那是文件系统层面的初始化（读目录、算空闲地址），不是硬件初始化。
 *   → SPI 硬件已经在 main() 里初始化好了，Task 里只是用。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │         第二章：如果 ReadID 真的在任务里执行，RTOS 有影响吗？    │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【假设场景】你把 ReadID 放到 StartUpTask 里了：
 *
 *   void StartUpTask(void *p) {
 *       uint32_t id = W25Q64_ReadID();               // ← 在任务里读 ID
 *       printf("ID=%X\n", id);                       // ← 可能在读一半时被切走！
 *   }
 *
 * 【分析：SPI 硬件会怎样？】
 *
 *   SPI 是一个硬件外设，不是你用 CPU 模拟的。CPU 被切走了，SPI 时钟
 *   照样跑、移位寄存器照样移位。就像洗衣机的定时器——你把电视机关了，
 *   洗衣机不会因此停下来。
 *
 *   但是！你的代码里有一个隐患：
 *
 *   uint8_t SPI_Flash_SendByte(uint8_t byte)
 *   {
 *       SPITimeout = SPIT_FLAG_TIMEOUT;
 *       while (SPI_I2S_GetFlagStatus(FLASH_SPIx, SPI_I2S_FLAG_TXE) == RESET)
 *       {
 *           if ((SPITimeout--) == 0)                    // ← ① 在这里被切走
 *               return SPI_TIMEOUT_UserCallback(0);     // → 返回 0xFF！！
 *       }
 *       SPI_I2S_SendData(FLASH_SPIx, byte);
 *
 *       SPITimeout = SPIT_FLAG_TIMEOUT;
 *       while (SPI_I2S_GetFlagStatus(FLASH_SPIx, SPI_I2S_FLAG_RXNE) == RESET)
 *       {
 *           if ((SPITimeout--) == 0)                    // ← ② 在这里被切走
 *               return SPI_TIMEOUT_UserCallback(1);     // → 返回 0xFF！！
 *       }
 *       return (uint8_t)SPI_I2S_ReceiveData(FLASH_SPIx);
 *   }
 *
 *   ★ 如果任务被切走太久，SPITimeout 减到 0，回调返回 0xFF！
 *     这就是你看到的 0xFFFFFF 的来源之一！
 *
 *   ┌───────────────────────────────────────────────────────────────┐
 *   │  正常情况下：                                                  │
 *   │  SPITimeout=4096 → 减 → 减 → ... → TXE=1（只减了几十次）     │
 *   │  不出问题。                                                    │
 *   │                                                                │
 *   │  被切走的情况：                                                │
 *   │  SPITimeout=4096 → 减到一半 → 【切走100ms】→ 回来继续减      │
 *   │  → 减到0 → 超时！→ 输出 "[SPI ERROR] Timeout! code=0"        │
 *   │  → 返回 0xFF                                                  │
 *   └───────────────────────────────────────────────────────────────┘
 *
 * 【vTaskDelay(20) 能治本吗？】
 *
 *   不能。vTaskDelay 只是让当前任务"主动休眠"20ms，回来之后照样
 *   可能被切走。它不能阻止任务切换——任务切换是 RTOS 的"心跳"，
 *   永远在后台运行。
 *
 *   打个比方：你刷牙刷到一半，闹钟响了（RTOS 切换），你去接电话。
 *   vTaskDelay 相当于你提前定了个 20ms 的"工作闹钟"，但没用——
 *   你正在刷牙的时候照样会有别人的电话打进来。
 *
 * 【正确的解决方案有两个】
 *
 *   方案A：关调度锁（最简单，单次操作用）
 *     void StartUpTask(void *p) {
 *         taskENTER_CRITICAL();                 // 关中断，禁止切换
 *         uint32_t id = W25Q64_ReadID();
 *         taskEXIT_CRITICAL();
 *         printf("ID=%X\n", id);
 *     }
 *
 *   方案B：DMA + 中断（最彻底，高频操作用）
 *     用 DMA 搬运 SPI 数据，硬件自动完成，不依赖 CPU 轮询。
 *     但这个太复杂，你目前不需要。
 *
 *   不过！你现在的代码 ReadID 在 main() 里，调度器还没启动，
 *   不需要任何保护！你的观点只对"未来的代码"有效。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │         第三章：0xFFFF 的真正原因 —— CS 信号排查                │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * W25Q64 的 MISO 引脚在 CS 高电平时是高阻态（Hi-Z）。
 * 在 STM32 的 GPIO 上，高阻态被上拉电阻拉到 VCC → 读到的是 1。
 * 所以如果你发了命令但 CS 没拉低，你收到的就是全 1 = 0xFF。
 *
 * 【检查清单】
 *
 *   ┌─── 硬件连接 ──────────────────────────────────────────┐
 *   │  CS  (PC0) → W25Q64 的 /CS 引脚                        │
 *   │  SCK (PA5) → W25Q64 的 CLK 引脚                        │
 *   │  MOSI(PA7) → W25Q64 的 DI  引脚                        │
 *   │  MISO(PA6) → W25Q64 的 DO  引脚                        │
 *   │                                                        │
 *   │  检查：                                                 │
 *   │  1. 用万用表量 PC0 引脚电平，调用 SPI_FLASH_CS_LOW()   │
 *   │     之后应该变成 0V，CS_HIGH() 之后变成 3.3V           │
 *   │                                                        │
 *   │  2. CS 引脚有没有接上拉电阻？没有也能工作，但有更好     │
 *   │                                                        │
 *   │  3. SCK 空闲时应该是高电平（因为 CPOL=1，模式3），     │
 *   │     用逻辑分析仪或示波器看                             │
 *   └────────────────────────────────────────────────────────┘
 *
 *   ┌─── SPI 模式 ──────────────────────────────────────────┐
 *   │  W25Q64 支持模式0 和模式3                              │
 *   │                                                        │
 *   │  你的代码设置：                                         │
 *   │    SPI_CPOL = High  (CPOL=1)                           │
 *   │    SPI_CPHA = 2Edge (CPHA=1)                           │
 *   │    → 这是 SPI 模式 3，没问题                           │
 *   │                                                        │
 *   │  如果 CPHA 配错（比如 1Edge），数据会在错误的时钟边沿  │
 *   │  采样，可能读到全 0 或全 1                             │
 *   └────────────────────────────────────────────────────────┘
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              第四章：推荐的诊断步骤                              │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 步骤1：加调试打印，确认 CS 动作
 *   在 SPI_Flash_ReadID() 的 CS_HIGH 之前加 printf，看是否输出了
 *
 * 步骤2：单独测试 CS 拉低
 *   写一个最简单的测试函数：
 *
 *   void Test_CS_Toggle(void) {
 *       printf("CS LOW  ...\r\n");
 *       SPI_FLASH_CS_LOW();
 *       for(volatile int i=0; i<100000; i++);  // 简单延时
 *       printf("CS HIGH ...\r\n");
 *       SPI_FLASH_CS_HIGH();
 *       for(volatile int i=0; i<100000; i++);
 *   }
 *
 *   // 循环调用，用示波器看 PC0 引脚有没有方波
 *
 * 步骤3：确认 SPI 时钟正常
 *   发送 0xFF 四个字节，用逻辑分析仪看 SCK 有没有 8x4=32 个脉冲
 *
 * 步骤4：降低 SPI 速度排除速率问题
 *   把 BaudRatePrescaler 从 4 改成 256（降到 281kHz），看 ReadID 是否正常
 *   如果降低速度后正常，说明高速率下信号完整性问题（线太长、无终端电阻等）
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │         总结                                                 │
 * └─────────────────────────────────────────────────────────────────┘
 *
 *   ┌───────┬───────────────────────────────────┬──────────────┐
 *   │ 原因  │ 现象                               │ 如何验证      │
 *   ├───────┼───────────────────────────────────┼──────────────┤
 *   │CS没拉低│ MISO=Hi-Z，读到全1 → 0xFFFFFF   │ 示波器量 PC0  │
 *   │SPI模式│ CPOL/CPHA配错 → 采样到全1        │ 检查初始化代码│
 *   │超时   │ 轮询超时，回调返回 0xFF           │ 串口看有无    │
 *   │       │                                   │ [SPI ERROR]   │
 *   │速度太快│高速下信号质量差                  │ 降低分频系数  │
 *   │多Dummy│ 命令后空闲字节不够               │ 多读一字节试试│
 *   └───────┴───────────────────────────────────┴──────────────┘
 *
 *   你当前代码 ReadID 在 main() 里，调度器未启动，不是 RTOS 的问题。
 *   优先排查：CS 是否真的拉低了。
 *
 * ============================================================================
 */