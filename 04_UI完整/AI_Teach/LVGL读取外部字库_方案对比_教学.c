/**
 * ============================================================================
 * LVGL 读取外部 W25Q64 Flash 字库 —— 方案对比与选型
 * ============================================================================
 *
 * 【背景】
 *   你的字库 .bin 文件已经通过串口烧录到 W25Q64 Flash 中（id=0, 约 2.5MB）。
 *   现在需要让 LVGL 能从 Flash 中读取字库数据，渲染到 ILI9341 屏幕上。
 *
 * 【关键限制】
 *   STM32F103VE RAM = 64KB，字库 .bin ≈ 2.5MB
 *   → 不可能把整个字库加载到 RAM，必须用外部读取方式。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │                  方案总览（4 种方案）                            │
 * └─────────────────────────────────────────────────────────────────┘
 *
 *   ┌──────┬─────────────────────┬─────────┬──────────┬──────────┐
 *   │ 方案 │ 名称                 │ RAM用量 │ 渲染速度  │ 实现难度 │
 *   ├──────┼─────────────────────┼─────────┼──────────┼──────────┤
 *   │  A   │ XBF 外部位图字体     │ 极小    │ ★★☆☆☆   │ ★★☆☆☆   │
 *   │  B   │ LVGL 文件系统 + FS   │ 中等    │ ★★★☆☆   │ ★★★☆☆   │
 *   │  C   │ 缓存式字体引擎       │ 可配置   │ ★★★★☆   │ ★★★★☆   │
 *   │  D   │ 预取字模 + 静态数组  │ 极大    │ ★★★★★   │ ★☆☆☆☆   │
 *   └──────┴─────────────────────┴─────────┴──────────┴──────────┘
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  方案 A：XBF 外部位图字体（你当前代码已采用，推荐保留）        │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【原理】
 *   LVGL v8 内置的 XBF (eXternal BitMap Font) 机制。
 *   你在 lv_font_t 结构体里提供两个回调函数指针，LVGL 在渲染时调用它们：
 *
 *     const lv_font_t my_font_SCH_16 = {
 *         .get_glyph_dsc    = __user_font_get_glyph_dsc,   ← 获取字形尺寸
 *         .get_glyph_bitmap = __user_font_get_bitmap,      ← 获取字形位图
 *     };
 *
 *   每次 LVGL 要画一个字符时：
 *      1. 调用 get_glyph_dsc() → 知道这个字多大、偏移多少
 *      2. 调用 get_glyph_bitmap() → 拿到位图数据
 *      3. LVGL 在位图数据的指针有效期内完成绘制
 *      4. 画完后释放（或覆盖这个指针）
 *
 * 【数据流向（你的代码）】
 *
 *   LVGL 渲染器
 *       │
 *       ├── get_glyph_dsc(unicode='中')
 *       │     └→ __user_font_getdata(offset, 10字节)
 *       │           └→ W25Q64_Read(flash_addr+offset, buf, 10)  ← SPI 读 Flash
 *       │           └→ 返回 buf 指针 → LVGL 解析 glyph_dsc_t
 *       │
 *       └── get_glyph_bitmap(unicode='中')
 *             └→ __user_font_getdata(pos, 128字节)               ← 再次 SPI 读 Flash
 *                   └→ W25Q64_Read(flash_addr+pos, buf, 128)
 *                   └→ 返回 buf 指针 → LVGL 绘制到屏幕
 *
 *   ★ 关键：每画一个字需要 2 次 W25Q64_Read()（共 2 次 SPI 通信）
 *
 * ╔══════════════════════════════════════════════════════════════════════════╗
 * ║  常见疑问：为什么不直接用 W25Q64_FileRead() 一次性读完？              ║
 * ╚══════════════════════════════════════════════════════════════════════════╝
 *
 *   W25Q64_FileRead() 和 W25Q64_Read() 的区别就像"搬整箱"和"拿一件"：
 *
 *   ┌─────────────────────────────────────────────────────────────────┐
 *   │  W25Q64_FileRead(id, buf, buf_size, &actual_size)              │
 *   │    你给一个 RAM 数组(buf)，我帮你把整个文件读到那个数组里。     │
 *   │    前提：你的 buf 必须 ≥ 文件总大小。                          │
 *   │    如果你的 buf 是 uint8_t buf[1024]，而文件是 2.4MB → 装不下！│
 *   └─────────────────────────────────────────────────────────────────┘
 *   VS
 *   ┌─────────────────────────────────────────────────────────────────┐
 *   │  W25Q64_Read(addr, buf, size)                                  │
 *   │    你自己算好地址，告诉我从 Flash 的哪个位置读多少字节。       │
 *   │    一次只读一小段（比如 128 字节），读完就用，下次覆盖。       │
 *   │    buf 只需够装"这一段"就行 → 128 字节的 buf 就够了！          │
 *   └─────────────────────────────────────────────────────────────────┘
 *
 *   具体到你的项目：
 *     - 字库文件 (id=0) 大小约 2.4MB
 *     - STM32F103VE RAM 64KB，用户可用 ≈ 40KB（FreeRTOS 等已占用一部分）
 *     - W25Q64_FileRead() 需要一次性用 2.4MB 的 buf → **不可能**
 *     - __g_font_buf[256] 只需 256 字节 → **稀疏地按需读取**，每次只读
 *       一个字形 (glyph) 的位图数据（16x16@4bpp = 128 字节）
 *
 *   代码证据（my_font_SCH_16.c 第 69~103 行）：
 *
 *      void my_font_SCH_16_init(void) {
 *          // 只查地址，不读全部数据！
 *          W25Q64_GetFileAddr(0, &__g_font_flash_addr, &size);
 *      }
 *
 *      static uint8_t *__user_font_getdata(int offset, int size) {
 *          // 每次只读 size 字节（最大 128），不是读整个 2.4MB 文件！
 *          W25Q64_Read(__g_font_flash_addr + offset, __g_font_buf, size);
 *          return __g_font_buf;
 *      }
 *
 *   总结一句话：
 *     W25Q64_FileRead 是"给足 RAM 才能用"的奢侈接口，
 *     W25Q64_Read 是"一次只拿一点点"的精打细算接口。
 *     当文件比 RAM 大时，只能用后者——这就是 XBF 字体引擎绕开
 *     文件系统层、直接用底层接口的根本原因。
 *
 * 【优点】
 *   1. 官方支持，接口标准，代码量少
 *   2. RAM 只占 256 字节（你的 __g_font_buf[256]），远小于 2.4MB 字库
 *   3. 已经写完并验证通过
 *   4. 不需要改 LVGL 源码
 *
 * 【缺点】
 *   1. 每画一个字都要通过 SPI 读 Flash，速度较慢
 *      （但 16x16 字形只有 128 字节，SPI 读约 1ms，一般场景够用）
 *   2. 大量文字时可能影响帧率（比如滚动一大段中文）
 *   3. 回调期间不能进行任务切换（因为是同步调用）
 *
 * 【优化方向（不需要大改）】
 *   a. 增大 __g_font_buf 到 512 或 1024（如果 RAM 允许）
 *      → 减少读取次数（一次多读几字节）
 *   b. 提高 SPI 时钟（当前 9MHz → 调到 18MHz）
 *      → 读取速度翻倍
 *   c. 在 get_glyph_dsc 中一起读出位图（合并两次 SPI 为一次）
 *      → 但还是得先知道 pos 才能读位图，顺序是固定的
 *
 * 【适用场景】
 *   ✓ 显示少量中文（如天气信息、时间日期）
 *   ✓ RAM 紧张的单片机项目
 *   ✓ 不需要频繁切换字体的场景
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  方案 B：LVGL 文件系统 + FS 驱动                                │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【原理】
 *   LVGL 内置了文件系统抽象层，你可以注册一个"文件系统驱动"。
 *   LVGL 认为它在读文件（fopen / fread / fseek），但实际上在后台
 *   是你在操作 W25Q64 Flash。
 *
 *   然后使用 lvgl 的在线字体转换器生成 .bin 字体文件，
 *   通过 lv_font_load("F:/myfont.bin") 动态加载字体。
 *
 * 【架构】
 *
 *   LVGL 字体系统
 *       │
 *       ├── lv_font_load("F:/fonts/song_16.bin")  ← LVGL 调用文件系统
 *       │
 *       └── 文件系统驱动（你写的 fs_w25q64.c）
 *             ├── fs_open()  → 从 W25Q64 目录找文件
 *             ├── fs_read()  → W25Q64_Read()
 *             ├── fs_seek()  → 计算 Flash 地址 + 偏移
 *             └── fs_close() → 空操作
 *
 *   注意：`lv_font_load()` 在 LVGL v8 中可能不存在（取决于配置），
 *   这个是 v9 的功能。v8 中文件系统的用途主要是图片（lv_img）。
 *
 * 【v8 中的实际用法】
 *   在 LVGL v8.2 中，文件系统主要用于：
 *     - lv_img_set_src(img, "F:/logo.bin")    ← 从 Flash 加载图片
 *     - lv_tileview 等需要资源文件的控件
 *
 *   字体在 v8 中主要还是通过 lv_font_t 结构体（方案A）来定义，
 *   不能像 v9 那样直接 `lv_font_load()` 动态加载。
 *
 * 【优点】
 *   1. LVGL 标准接口，和控件系统无缝集成
 *   2. 可以统一管理所有外部资源（字库、图片、配置文件）
 *   3. 一套 FS 驱动，所有 LVGL 控件都能用
 *
 * 【缺点】
 *   1. 在 LVGL v8.2 中，字体不支持通过 FS 动态加载（需要验证）
 *   2. 需要注册 FS 驱动（虽然代码量不大）
 *   3. 实际上和方案 A 的 W25Q64_Read() 是一样的底层操作
 *   4. 多了一层抽象，可能有微小性能开销
 *
 * 【实现步骤（如果你选这个方案）】
 *   1. 实现 lv_fs_drv_t 结构体的回调函数
 *   2. 在 main 中注册：lv_fs_drv_register(&my_fs_drv)
 *   3. 字体文件仍然需要方案 A 的 get_glyph_bitmap 回调
 *      （因为 v8 不能直接从 FS 加载字体）
 *
 * 【结论】
 *   在 LVGL v8.2 中，方案 B 主要用于图片加载，对字体的帮助不大。
 *   如果以后升级到 LVGL v9，可以考虑完全改用 FS 方式。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  方案 C：缓存式字体引擎                                          │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【原理】
 *   方案 A 每画一个字都要读 Flash。如果一屏有 50 个中文字，
 *   那就是 100 次 SPI 通信（50×2），耗时约 100ms——还能接受，
 *   但不算快。
 *
 *   方案 C 在方案 A 基础上加一层缓存：
 *
 *   ┌──────────────────────────────────────────────────────────┐
 *   │                    缓存表 (RAM)                          │
 *   │  ┌────────┬──────────────┬──────────────────────┐       │
 *   │  │Unicode │  glyph_dsc   │   bitmap (128B)      │       │
 *   │  ├────────┼──────────────┼──────────────────────┤       │
 *   │  │ 0x4E2D │ {16,16,...}  │ [128 bytes 位图]     │ ←'中' │
 *   │  │ 0x56FD │ {16,16,...}  │ [128 bytes 位图]     │ ←'国' │
 *   │  │  ...   │   ...        │   ...                │       │
 *   │  └────────┴──────────────┴──────────────────────┘       │
 *   │         每个缓存条目 ≈ 140 字节                          │
 *   │         10 条缓存 ≈ 1.4KB RAM                           │
 *   └──────────────────────────────────────────────────────────┘
 *
 *   渲染流程：
 *     get_glyph_dsc(unicode) {
 *         if (unicode 在缓存中)
 *             return 缓存的 glyph_dsc;          ← 0 次 SPI！
 *         else {
 *             从 Flash 读取 glyph_dsc;           ← 1 次 SPI
 *             加入缓存;
 *             return;
 *         }
 *     }
 *
 * 【缓存替换策略】
 *   可以用简单的 LRU（最近最少使用）或 FIFO（先进先出）。
 *   对于天气时钟这种场景，显示的字是固定的（"温度""湿度""城市名"等），
 *   缓存命中率极高（>90%），第一次画完后就全在缓存里了。
 *
 * 【优点】
 *   1. 大量减少 Flash 读取次数（缓存命中时 0 次 SPI）
 *   2. 天气时钟场景的缓存命中率极高
 *   3. 代码量不大（在方案 A 基础上加 ~50 行）
 *
 * 【缺点】
 *   1. 需要额外 RAM（每条缓存约 140 字节，10 条 ≈ 1.4KB）
 *   2. 代码稍复杂（需要管理缓存表）
 *   3. 缓存满了需要淘汰策略
 *
 * 【适用场景】
 *   ✓ 显示内容相对固定（天气、时钟、菜单）
 *   ✓ 对流畅度有要求
 *   ✓ 有 1~3KB 空闲 RAM
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  方案 D：预取字模 + 静态数组（不适用于你的场景）               │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【原理】
 *   在编译时就确定需要哪些字，把它们的字模数据写成 C 数组，
 *   编译到 ROM（Flash 内部）中。运行时不需要读外部 Flash。
 *
 *   static LV_ATTRIBUTE_LARGE_CONST const uint8_t glyph_bitmap_4e2d[] = {
 *       0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,...
 *   };
 *
 * 【为什么不适合你】
 *   STM32F103VE 内部 Flash = 512KB
 *   一个 GB2312 全字库（6763 字）的字模数据 ≈ 2.5MB
 *   → 内部 Flash 装不下！
 *
 *   即使只取常用字（比如 1000 个），也需要 1000 × 128B = 128KB
 *   → 代码区已经占了不少，剩余空间不够
 *
 *   ★ 外部 Flash (W25Q64) 才是字库的正确归宿
 *
 * 【适用场景】
 *   ✓ 内部 Flash > 1MB 的型号（如 STM32H743）
 *   ✓ 只需要少量字符（如英文 ASCII，只有 128 个字）
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              最终推荐：方案 A 起步，按需升级到方案 C            │
 * └─────────────────────────────────────────────────────────────────┘
 *
 *   你的 my_font_SCH_16.c 已经实现了方案 A，而且是正确的！
 *   先用它跑起来，验证功能没问题。等以后发现滚动文字掉帧时，
 *   再在方案 A 基础上加缓存（方案 C），改动量很小。
 *
 * 【升级到方案 C 的改动清单（未来参考）】
 *
 *   1. 定义缓存结构体（~20 行）
 *   2. 定义缓存数组（~5 行）
 *   3. 实现缓存查找函数（~15 行）
 *   4. 修改 __user_font_getdata() —— 先查缓存，miss 了再读 Flash（~10 行）
 *
 *   总共不到 100 行代码，改动只在一个文件内。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  进阶：如何在同一个项目中接入多种字体？                        │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 假设你以后还要加入 24x24、32x32 字体，每种字体一个 .bin：
 *   id=0 → SCH_16.bin   (2.5MB)
 *   id=1 → SCH_24.bin   (更大)
 *   id=2 → SCH_32.bin   (更大)
 *
 * W25Q64 只有 8MB，放不下全部三种（需要约 20MB）。
 * 但假设你只放一种大字体 + 一种小字体，需要解决的是：
 *
 *   每个 lv_font_t 对象需要在 Flash 中指向不同的起始地址。
 *
 *   方案：给每个字体单独写一个 init 函数和回调函数
 *
 *   // my_font_SCH_16.c
 *   static uint32_t g_addr_16 = 0;
 *   void font_16_init(void) { W25Q64_GetFileAddr(0, &g_addr_16, ...); }
 *   static uint8_t* font_16_getdata(int off, int sz) {
 *       W25Q64_Read(g_addr_16+off, buf, sz);
 *   }
 *
 *   // my_font_SCH_32.c（结构相同，只有 g_addr_32 不同）
 *   static uint32_t g_addr_32 = 0;
 *   void font_32_init(void) { W25Q64_GetFileAddr(1, &g_addr_32, ...); }
 *   static uint8_t* font_32_getdata(int off, int sz) {
 *       W25Q64_Read(g_addr_32+off, buf, sz);
 *   }
 *
 *   然后用同一个 __g_font_buf 即可（因为同一时刻只有一个字被渲染）。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  当前你需要做什么？—— 什么代码都不用改，直接验证即可          │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 你的 my_font_SCH_16.c 已经是方案 A 的完整实现，结构正确。
 *
 * 验证步骤：
 *
 * 1. 确认 my_font_SCH_16_init() 在 W25Q64_FileSysInit() 之后调用
 *    → 你的 main.c 里已经是这个顺序 ✓
 *
 * 2. 确认 lv_conf.h 中字体配置正确
 *    → LV_FONT_MONTSERRAT_* 可以全关掉节省 Flash
 *    → 因为你用的是外部字体
 *
 * 3. 在 LVGL 中使用：
 *    lv_obj_t *label = lv_label_create(lv_scr_act());
 *    lv_obj_set_style_text_font(label, &my_font_SCH_16, 0);
 *    lv_label_set_text(label, "你好世界");
 *
 * 4. 编译、烧录，观察屏幕是否显示中文
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              总结速查表                                         │
 * └─────────────────────────────────────────────────────────────────┘
 *
 *   你的代码 → 方案 A (XBF 外部位图) ← 正确！保留！
 *
 *   如果卡顿 → 升级到方案 C（加缓存）
 *   如果多字体 → 每种字体写一个文件，分别 init
 *   如果要管理图片 → 加方案 B（FS 驱动，用于 lv_img）
 *   如果换大芯片 → 方案 D（内部 Flash 放字体）
 *
 *   当前阶段：先用方案 A 把中文显示出来，其他以后再说。
 *
 * ============================================================================
 *
 *
 * ╔══════════════════════════════════════════════════════════════════════════╗
 * ║                                                                          ║
 * ║   ★★★  最终选型确认：方案 A — XBF 外部位图字体  ★★★                    ║
 * ║                                                                          ║
 * ║   决策日期：2025.05.23                                                   ║
 * ║                                                                          ║
 * ║   场景：天气时钟，静态显示几个中文字符（温度/湿度/城市名等）              ║
 * ║                                                                          ║
 * ║   选型理由：                                                              ║
 * ║     1. 当前只显示少量汉字，不需要缓存优化                                  ║
 * ║     2. 方案 A 已经实现完毕（my_font_SCH_16.c），无需额外开发              ║
 * ║     3. 使用方法与 LVGL 内置字体完全一样：                                 ║
 * ║           lv_obj_set_style_text_font(label, &my_font_SCH_16, 0);         ║
 * ║     4. 以后需要更多字体时，按同样模式写第2个字体文件即可                   ║
 * ║                                                                          ║
 * ║   代码文件：UI/Font_SCh_16/my_font_SCH_16.c                               ║
 * ║   配套驱动：Driver/W25Q64.c  +  Driver/W25Q64.h                           ║
 * ║                                                                          ║
 * ╚══════════════════════════════════════════════════════════════════════════╝
 *
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │  方案 A 使用速查 —— 像用 LVGL 自带字体一样用                   │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【初始化（main.c，务必在 W25Q64_FileSysInit 之后调用）】
 *
 *     W25Q64_Init();
 *     W25Q64_FileSysInit();      // ← 扫描目录区
 *     my_font_SCH_16_init();     // ← 从目录找到 id=0 的字库文件地址
 *     lv_init();
 *     // ... 其他初始化 ...
 *
 * 【声明外部字体（使用时 #include 对应的 .h）】
 *
 *     extern const lv_font_t my_font_SCH_16;
 *
 * 【创建 Label 并设置为中文字体】
 *
 *     // 方式一：创建时就指定
 *     lv_obj_t *label = lv_label_create(lv_scr_act());
 *     lv_obj_set_style_text_font(label, &my_font_SCH_16, 0);
 *     lv_label_set_text(label, "你好世界");
 *
 *     // 方式二：设置全局默认字体（所有 Label 都用）
 *     lv_style_t style;
 *     lv_style_init(&style);
 *     lv_style_set_text_font(&style, &my_font_SCH_16);
 *     lv_obj_add_style(lv_scr_act(), &style, 0);
 *
 * 【换用英文数字时（可继续用 LVGL 自带字体，更省 SPI 带宽）】
 *
 *     lv_obj_set_style_text_font(label, &lv_font_montserrat_14, 0);
 *     lv_label_set_text(label, "23.5°C");
 *
 * 【注意事项】
 *    1. get_glyph_bitmap 回调返回的指针只在 LVGL 本次绘制期间有效，
 *       下次调用 getdata 时会覆盖 __g_font_buf，这是正常行为。
 *    2. 不要在中断里调用 W25Q64_Read()，会被 LVGL 字体回调重入。
 *    3. SPI 总线被字体读取独占期间，其他 SPI 设备（触摸屏）需等待。
 *
 * ============================================================================
 */
