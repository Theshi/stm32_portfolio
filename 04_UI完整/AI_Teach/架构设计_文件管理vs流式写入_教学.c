/**
 * ============================================================================
 * 架构设计讲解：为什么"文件管理层"和"流式写入层"要分开写？
 * ============================================================================
 *
 * 【一句话答案】
 *   文件管理层假设"数据已经在 RAM 里"，流式层解决的是"数据在串口上，
 *   来一个字节写一个字节，RAM 只有 256 字节缓冲区"。
 *   如果这两层不分家，你就被迫把所有数据全读到 RAM——这不可能，因为
 *   字库 2.5MB 远超 STM32F103 的 64KB RAM。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │           第一章：先搞懂"同步"和"异步"的本质区别                 │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【类比：做饭】
 *
 *   方案A（文件管理 = 同步写入）：
 *     你有一筐菜，站在灶台前，一口气全炒完。
 *     → 前提：整筐菜必须在手边（数据全在 RAM 里）
 *     → 适合：数据量 ≤ RAM 容量
 *
 *   方案B（流式写入 = 异步写入）：
 *     菜市场一根一根送菜过来，你收到一根就切一根、炒一根。
 *     → 前提：只需要一个盘子当临时缓冲区（256 字节）
 *     → 适合：数据来自外部（串口），大小远超 RAM
 *
 * 【技术对应】
 *
 *   ┌──────────────────────────────────────────────────────────────┐
 *   │                                                               │
 *   │   W25Q64_WriteFile(id, buf, size)    ← 同步（文件管理层）     │
 *   │   参数：buf 指向 RAM 中完整的 2.5MB 数据                      │
 *   │   问题：STM32 根本没有 2.5MB RAM 来存这份数据！               │
 *   │                                                               │
 *   │   StreamStart → StreamFeed → StreamEnd  ← 异步（流式层）      │
 *   │   数据来源：串口中断，每次收一个字节                            │
 *   │   缓冲区：只占 256 字节（一页 Flash 的大小）                   │
 *   │                                                               │
 *   └──────────────────────────────────────────────────────────────┘
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              第二章：什么是"分层的职责不同"                      │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 你的代码里有三层，每层只管自己的事：
 *
 *   ┌──────────────────────────────────────────────────────┐
 *   │                                                       │
 *   │  第3层: 串口协议层 (serial_flash_loader.c)            │
 *   │  ├─ 解析 fireTools 发来的命令包                       │
 *   │  ├─ 从串口逐字节收到数据                              │
 *   │  └─ 调用第2层 StreamFeed(byte) 喂进去                │
 *   │                                                       │
 *   │  第2层: 流式写入层 (W25Q64 Stream API)                │
 *   │  ├─ 维护 256 字节页缓冲区                             │
 *   │  ├─ 攒满一页 → 调用第1层烧 Flash                      │
 *   │  └─ 最后的 StreamEnd 调用第1层登记目录                │
 *   │                                                       │
 *   │  第1层: 文件管理层 (W25Q64 Core API)                  │
 *   │  ├─ 目录读写、空间计算                                │
 *   │  ├─ 扇区擦除、页写入（底层 SPI）                      │
 *   │  └─ 不关心数据从哪来，只管"写进 Flash"               │
 *   │                                                       │
 *   └──────────────────────────────────────────────────────┘
 *
 * 【核心原则：每层只做一件事】
 *
 *   第1层不管数据从哪来（RAM? 串口? SD卡?），它只负责:
 *     - 擦除扇区
 *     - 写入一页
 *     - 登记目录
 *
 *   第2层不管 Flash 怎么擦除，它只负责:
 *     - 把分散到来的字节攒成 256 字节一页
 *     - 到一页了就叫第1层写进去
 *
 *   第3层不管数据怎么落到 Flash，它只负责:
 *     - 跟 fireTools 握手通信
 *     - 把收到的每个字节喂给第2层
 *
 *   如果这三层混在一起写，代码就会变成意大利面条——改串口协议
 *   要动 Flash 底层，改 Flash 页大小要动串口逻辑。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │         第三章：流式写入 vs 文件管理函数的本质区别              │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【对比表】
 *
 *   ┌───────────────────────┬──────────────────────┬──────────────────┐
 *   │       特性            │  文件管理层          │  流式写入层      │
 *   ├───────────────────────┼──────────────────────┼──────────────────┤
 *   │ 数据来源              │  RAM 数组            │  串口/网络/外设  │
 *   │ 数据到达方式          │  一次性全在内存里    │  逐字节到来      │
 *   │ 调用方式              │  调用一次，等着返回  │  分三步调用      │
 *   │ 所需 RAM              │  必须 ≥ 文件大小     │  只需 256 字节   │
 *   │ 写入前是否知道大小    │  知道（参数传入）    │ StreamStart 时知道│
 *   │ CRC 校验              │  全部写完一起算      │  写完再算(或跳过)│
 *   │ 适用场景              │  RAM 够用的场景      │  大数据烧录      │
 *   │ 函数签名              │  write(id,buf,size)  │  Start→Feed→End  │
 *   └───────────────────────┴──────────────────────┴──────────────────┘
 *
 * 【你代码里的实际区别】
 *
 *  // ── 文件管理层（数据在 RAM 里）──
 *  uint8_t small_config[128] = {...};  // 128 字节的小配置
 *  W25Q64_WriteFile(0, small_config, 128);  // ✅ 一次调用就完成
 *
 *  // ── 流式写入层（数据从串口来）──
 *  // 假设串口要收 2.5MB 数据，RAM 只有 64KB
 *  // ❌ 不能这样写：
 *  //   uint8_t huge_buf[2527232];  // 编译不过！栈溢出！
 *  //   W25Q64_WriteFile(0, huge_buf, 2527232);
 *
 *  // ✅ 正确做法：
 *  SerialFlashLoader_Start(0, 2527232);  // 第1步：准备
 *  // 串口中断里每收到一个字节:
 *  SerialFlashLoader_Feed(byte);         // 第2步：逐字节喂入
 *  // 全部收完:
 *  SerialFlashLoader_End();              // 第3步：收尾+目录回写
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              第四章：工程架构上值得学习的地方                    │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 【1. 分层解耦（Layered Architecture）】
 *
 *   ┌──────────────┐
 *   │  应用层      │  main.c → 决定"什么时候烧录"、"烧什么"
 *   ├──────────────┤
 *   │  协议层      │  serial_flash_loader.c → 解析 fireTools 命令
 *   ├──────────────┤
 *   │  流式层      │  W25Q64 Stream API → 攒字节、切页
 *   ├──────────────┤
 *   │  文件管理层  │  W25Q64 Core API → 目录管理、空间计算
 *   ├──────────────┤
 *   │  硬件抽象层  │  bsp_spi.c → SPI 收发、CS 控制
 *   └──────────────┘
 *
 *   好处：改任何一层都不影响其他层。
 *   比如你想把串口协议换成 SD 卡读取，只需要改协议层，
 *   下面四层完全不用动。
 *
 * 【2. 接口隔离（Interface Segregation）】
 *
 *   文件管理层提供"完整写入"接口 → 给那些数据已经在 RAM 里的场景用
 *   流式层提供"分步写入"接口 → 给数据逐字节到来的场景用
 *
 *   两个接口各司其职，互不干扰。
 *   如果你把两者混成一个接口，使用者就分不清"这个函数要不要
 *   先把数据全读出来"。
 *
 * 【3. 状态机模式】
 *
 *   StreamStart → StreamFeed(循环) → StreamEnd
 *        ↓              ↓                ↓
 *    IDLE 态 →   FEEDING 态 →    DONE 态
 *
 *   状态机的好处：
 *     - 写一半断电了？Stream 变量丢失，目录不会登记半成品
 *     - 中途串口断了？StreamEnd 不会被调用，Flash 里不留脏数据
 *     - 可以随时中止：设置一个 abort 标志，Feed 里检测到就退出
 *
 * 【4. 缓冲区大小 = 硬件特性】
 *
 *   流式层的页面缓冲区是 256 字节，这不是随便定的——
 *   W25Q64 一页就是 256 字节。缓冲区大小和 Flash 页大小对齐，
 *   意味着"攒满一页 → 正好写一页"，没有任何浪费。
 *
 *   如果你把缓冲区设成 200 字节，那就悲剧了：
 *     - 攒满 200 字节 → 写了 200 字节（但 Flash 一页能写 256）
 *     - 下一轮又是从新地址开始（如果跨页还要多擦一次）
 *     - Flash 利用率直接打八折
 *
 * 【5. 单一数据源（Single Source of Truth）】
 *
 *   目录信息只在"文件管理层"里维护（g_DirCache 数组）。
 *   流式层不直接操作目录，它调用文件管理层来登记。
 *   如果两处都能改目录，迟早会出数据不一致的 bug。
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              第五章：如果硬要合并会怎样？                        │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * 假设你把文件管理层和流式层写进同一个函数 `W25Q64_WriteFile`，
 * 让它能同时处理"RAM 数组"和"串口数据"：
 *
 *   void W25Q64_WriteFile(int id, void* data, int size, int source_type)
 *   {
 *       if (source_type == FROM_RAM) {
 *           // 数据在 RAM 里，一次写完
 *           ...
 *       } else if (source_type == FROM_UART) {
 *           // 数据在串口上，要等中断...
 *           // 但这是个同步函数！调用者期望它执行完就结束！
 *           // 你不能在这里死等串口数据——那会阻塞整个系统！
 *       }
 *   }
 *
 * 问题：
 *   1. 函数职责不清晰——一个函数做了两件完全不同的事
 *   2. 异步数据源无法在同步函数里处理
 *   3. 每增加一种数据来源（SD卡？CAN总线？）就要改这个函数
 *   4. 测试困难——测试串口路径必须真的接上串口
 *
 * ============================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │              总结：一张图把你代码的架构全串起来                  │
 * └─────────────────────────────────────────────────────────────────┘
 *
 *   main.c 启动任务
 *       │
 *       ▼
 *   SerialFlashLoader_Start(id=0, size=2527232)   ← 协议层
 *       │                                             计算起始地址
 *       │                                             初始化 Stream 状态机
 *       ▼
 *   USART1 中断 → 每收一个字节调用 StreamFeed(byte)  ← 流式层
 *       │                                             256字节页缓冲区
 *       │                                             到一页 → 调用底层
 *       ▼
 *   W25Q64_SectorErase / PageProgram                ← 文件管理层
 *       │                                             扇区擦除懒策略
 *       │                                             实际写 Flash
 *       ▼
 *   SPI_Flash_SendByte / CS 控制                    ← 硬件抽象层
 *       │                                             SPI 收发
 *       │                                             片选拉高拉低
 *       ▼
 *   W25Q64 芯片
 *
 *   ───────────────────────────────────────────────────
 *   所有数据写完，调用 StreamEnd
 *       │
 *       ▼
 *   登记目录 → 擦除目录区 → 写入新目录 → 完成！
 *
 *   【关键理解】
 *   文件管理层(W25Q64_WriteFile) = 假设数据已经在 RAM，同步一次调完
 *   流式层(StreamStart/Feed/End) = 数据逐字节来，异步分步调用
 *   两者组合 = 在 64KB RAM 的单片机上烧录任意大小的文件到 Flash
 *
 * ============================================================================
 */